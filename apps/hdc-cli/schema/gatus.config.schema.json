{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://hdc.local/schema/gatus.config.schema.json",
  "title": "HDC gatus service package config",
  "type": "object",
  "additionalProperties": true,
  "required": ["schema_version", "deployments"],
  "properties": {
    "schema_version": { "type": "integer", "minimum": 2 },
    "defaults": { "type": "object" },
    "deployments": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["system_id"],
        "properties": {
          "system_id": { "type": "string", "pattern": "^gatus-[a-z]+$" },
          "mode": { "enum": ["proxmox-lxc"] },
          "proxmox": { "type": "object" },
          "install": {
            "type": "object",
            "properties": {
              "enabled": { "type": "boolean" }
            }
          },
          "gatus": {
            "type": "object",
            "properties": {
              "version": { "type": "string" },
              "listen_port": { "type": "integer", "minimum": 1, "maximum": 65535 },
              "config_path": { "type": "string" },
              "endpoints": { "type": "array" },
              "config_yaml_extra": { "type": ["string", "null"] }
            }
          }
        }
      }
    }
  }
}
