{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://hdc.local/schema/postiz.config.schema.json",
  "title": "HDC postiz service package config",
  "type": "object",
  "additionalProperties": true,
  "required": ["schema_version", "deployments"],
  "properties": {
    "schema_version": { "type": "integer", "minimum": 2 },
    "defaults": { "type": "object" },
    "deployments": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["system_id"],
        "properties": {
          "system_id": { "type": "string", "pattern": "^postiz-[a-z]+$" },
          "mode": { "enum": ["proxmox-lxc"] },
          "proxmox": { "type": "object" },
          "postiz": {
            "type": "object",
            "properties": {
              "version": { "type": "string" },
              "public_url": { "type": ["string", "null"] },
              "listen_port": { "type": "integer", "minimum": 1, "maximum": 65535 },
              "storage_provider": { "type": "string" },
              "db_password_vault_key": { "type": "string" },
              "jwt_secret_vault_key": { "type": "string" },
              "env_extra": { "type": "object" }
            }
          },
          "install": {
            "type": "object",
            "properties": {
              "enabled": { "type": "boolean" },
              "app_dir": { "type": "string" }
            }
          }
        }
      }
    }
  }
}
