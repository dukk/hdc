{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://hdc.local/schema/yacy.config.schema.json",
  "title": "HDC yacy service package config",
  "type": "object",
  "additionalProperties": true,
  "required": ["schema_version", "deployments"],
  "properties": {
    "schema_version": { "type": "integer", "minimum": 2 },
    "defaults": { "type": "object" },
    "deployments": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["system_id"],
        "properties": {
          "system_id": { "type": "string", "pattern": "^yacy-[a-z]+$" },
          "mode": { "enum": ["proxmox-lxc"] },
          "proxmox": { "type": "object" },
          "yacy": {
            "type": "object",
            "properties": {
              "image_tag": { "type": "string" },
              "http_port": { "type": "integer", "minimum": 1, "maximum": 65535 },
              "https_port": { "type": "integer", "minimum": 1, "maximum": 65535 },
              "peer_name": { "type": "string" },
              "public_url": { "type": ["string", "null"] },
              "admin_password_vault_key": { "type": "string" }
            }
          },
          "install": {
            "type": "object",
            "properties": {
              "enabled": { "type": "boolean" },
              "compose_dir": { "type": "string" }
            }
          }
        }
      }
    }
  }
}
