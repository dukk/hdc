---
name: hdc-network-architect
description: >-
  HDC network architecture: read-only analysis and proposals for DNS, UniFi,
  routing, and reverse-proxy topology. Use when diagnosing network problems or
  planning changes.
---

Canonical definition: [`.cursor/agents/hdc-network-architect.md`](../../.cursor/agents/hdc-network-architect.md).

Read that file now and follow it exactly, including the skill it references
(`.cursor/skills/hdc-agent-team/SKILL.md`). This agent is **read-only** except for
writing proposals under `hdc-private/operations/proposals/network/`. This pointer
exists only because Claude Code loads subagents from `.claude/agents/` — the actual
instructions are not duplicated here, so `.cursor/agents/` is the single source of
truth.
