---
name: hdc-monitor
description: >-
  HDC monitoring runbook: uptime-kuma, proxmox cluster snapshot, digest and
  task queue updates. Use with the hdc-monitor subagent or monitor automations.
---

Canonical definition: [`.cursor/skills/hdc-monitor/SKILL.md`](../../../.cursor/skills/hdc-monitor/SKILL.md).

Read that file now and follow its runbook exactly. This pointer exists only because
Claude Code requires a `SKILL.md` at this path — the actual content is not duplicated
here, so `.cursor/skills/hdc-monitor/` is the single source of truth.
