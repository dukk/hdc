# Copy to packages/infrastructure/azure-compute/.env in hdc-private (or hdc root; never commit).
# Azure compute — values optional unless manifest env_required is set.
# Prefer vault: node tools/hdc/cli.mjs secrets set <KEY>

# HDC_AZURE_COMPUTE_CLIENT_ID=
# HDC_AZURE_COMPUTE_SUBSCRIPTION_ID=
# HDC_AZURE_COMPUTE_TENANT_ID=
