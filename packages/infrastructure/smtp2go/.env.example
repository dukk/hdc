# Copy to packages/infrastructure/smtp2go/.env in hdc-private (or hdc root; never commit).
# Values are optional unless the package manifest declares env_required.

# SMTP2GO API (`packages/infrastructure/smtp2go`) — sender domains; separate from postfix-relay SMTP creds.
# node tools/hdc/cli.mjs secrets set HDC_SMTP2GO_API_KEY
# HDC_SMTP2GO_API_KEY=
