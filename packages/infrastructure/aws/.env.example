# Copy to packages/infrastructure/aws/.env in hdc-private (or hdc root; never commit).
# Values are optional unless the package manifest declares env_required.

# AWS infrastructure (`packages/infrastructure/aws`) — IAM access key id in .env; secret in vault.
# HDC_AWS_ACCESS_KEY_ID=
# node tools/hdc/cli.mjs secrets set HDC_AWS_SECRET_ACCESS_KEY
# HDC_AWS_SECRET_ACCESS_KEY=
# Optional STS session token for assumed roles:
# node tools/hdc/cli.mjs secrets set HDC_AWS_SESSION_TOKEN
