# Copy to packages/infrastructure/unifi-network/.env in hdc-private (or hdc root; never commit).
# UniFi Network — values optional unless manifest env_required is set.
# Prefer vault: node tools/hdc/cli.mjs secrets set <KEY>

# HDC_UNIFI_NETWORK_API_KEY=
# HDC_UNIFI_TLS_INSECURE=
