# Copy to packages/services/keepalived/.env in hdc-private (or hdc root; never commit).
# Keepalived VRRP and LVS load balancing — values optional unless manifest env_required is set.
# Prefer vault: node tools/hdc/cli.mjs secrets set <KEY>

# HDC_KEEPALIVED_AUTH_PASS=
