# Copy to packages/services/paperless-ngx/.env in hdc-private (or hdc root; never commit).
# Paperless-ngx document management — values optional unless manifest env_required is set.
# Prefer vault: node tools/hdc/cli.mjs secrets set <KEY>

# HDC_PAPERLESS_DB_PASSWORD=
# HDC_PAPERLESS_SECRET_KEY=
