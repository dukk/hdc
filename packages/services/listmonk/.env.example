# Copy to packages/services/listmonk/.env in hdc-private (or hdc root; never commit).
# Listmonk newsletter manager — values optional unless manifest env_required is set.
# Prefer vault: node tools/hdc/cli.mjs secrets set <KEY>

# HDC_LISTMONK_ADMIN_PASSWORD=
# HDC_LISTMONK_DB_PASSWORD=
