# Copy to packages/services/asterisk/.env in hdc-private (or hdc root; never commit).
# Asterisk PBX (PJSIP) — values optional unless manifest env_required is set.
# Prefer vault: node tools/hdc/cli.mjs secrets set <KEY>

# HDC_TWILIO_SIP_PASSWORD=
# HDC_TWILIO_SIP_USERNAME=
