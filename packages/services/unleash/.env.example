# Copy to packages/services/unleash/.env in hdc-private (or hdc root; never commit).
# Unleash feature flags — values optional unless manifest env_required is set.
# Prefer vault: node tools/hdc/cli.mjs secrets set <KEY>

# HDC_UNLEASH_ADMIN_PASSWORD=
# HDC_UNLEASH_DB_PASSWORD=
