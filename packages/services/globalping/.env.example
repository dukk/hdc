# Copy to packages/services/globalping/.env in hdc-private (or hdc root; never commit).
# Globalping network probe — values optional unless manifest env_required is set.
# Prefer vault: node tools/hdc/cli.mjs secrets set <KEY>

# HDC_GLOBALPING_ADOPTION_TOKEN=
