# Copy to packages/services/shlink/.env in hdc-private (or hdc root; never commit).
# Shlink URL shortener — values optional unless manifest env_required is set.
# Prefer vault: node tools/hdc/cli.mjs secrets set <KEY>

# HDC_SHLINK_DB_PASSWORD=
# HDC_SHLINK_INITIAL_API_KEY=
