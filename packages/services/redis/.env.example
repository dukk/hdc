# Copy to packages/services/redis/.env in hdc-private (or hdc root; never commit).
# Values are optional unless the package manifest declares env_required.

# Redis Cluster (`packages/services/redis`) — prefer vault over env.
# node tools/hdc/cli.mjs secrets set HDC_REDIS_PASSWORD
# HDC_REDIS_PASSWORD=
