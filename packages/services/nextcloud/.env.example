# Copy to packages/services/nextcloud/.env in hdc-private (or hdc root; never commit).
# Values are optional unless the package manifest declares env_required.

# Nextcloud AIO (packages/services/nextcloud): no vault secrets for v1 — admin/domain via AIO UI on :8080
