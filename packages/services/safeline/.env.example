# Copy to packages/services/safeline/.env in hdc-private (or hdc root; never commit).
# SafeLine WAF — values optional unless manifest env_required is set.
# Prefer vault: node tools/hdc/cli.mjs secrets set <KEY>

# HDC_SAFELINE_ADMIN_PASSWORD=
# HDC_SAFELINE_API_TOKEN=
# HDC_SAFELINE_POSTGRES_PASSWORD=
