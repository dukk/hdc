# Copy to packages/services/affine/.env in hdc-private (or hdc root; never commit).
# AFFiNE workspace — values optional unless manifest env_required is set.
# Prefer vault: node tools/hdc/cli.mjs secrets set <KEY>

# HDC_AFFINE_DB_PASSWORD=
