# Copy to packages/services/plex/.env in hdc-private (or hdc root; never commit).
# Plex Media Server (Synology native package) — values optional unless manifest env_required is set.
# Prefer vault: node tools/hdc/cli.mjs secrets set <KEY>

# No documented HDC_* env vars for this package (vault-only or config.json).
