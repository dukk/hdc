# Copy to packages/services/windows-desktop/.env in hdc-private (or hdc root; never commit).
# Windows 11 desktop (Proxmox QEMU) — values optional unless manifest env_required is set.
# Prefer vault: node tools/hdc/cli.mjs secrets set <KEY>

# HDC_WINDOWS_DESKTOP_ADMIN_PASSWORD=
