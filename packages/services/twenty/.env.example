# Copy to packages/services/twenty/.env in hdc-private (or hdc root; never commit).
# Twenty CRM — values optional unless manifest env_required is set.
# Prefer vault: node tools/hdc/cli.mjs secrets set <KEY>

# HDC_TWENTY_DB_PASSWORD=
# HDC_TWENTY_ENCRYPTION_KEY=
