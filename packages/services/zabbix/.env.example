# Copy to packages/services/zabbix/.env in hdc-private (or hdc root; never commit).
# Zabbix monitoring stack — values optional unless manifest env_required is set.
# Prefer vault: node tools/hdc/cli.mjs secrets set <KEY>

# HDC_ZABBIX_DB_PASSWORD=
