# Copy to packages/services/kali-desktop/.env in hdc-private (or hdc root; never commit).
# Kali Linux desktop (Proxmox QEMU) — values optional unless manifest env_required is set.
# Prefer vault: node tools/hdc/cli.mjs secrets set <KEY>

# HDC_KALI_DESKTOP_PASSWORD=
