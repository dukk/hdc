# Copy to packages/services/cloudbeaver/.env in hdc-private (or hdc root; never commit).
# CloudBeaver Community database manager — values optional unless manifest env_required is set.
# Prefer vault: node tools/hdc/cli.mjs secrets set <KEY>

# HDC_CLOUDBEAVER_ADMIN_PASSWORD=
