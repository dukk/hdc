# Copy to packages/services/valkey/.env in hdc-private (or hdc root; never commit).
# Values are optional unless the package manifest declares env_required.

# Valkey Cluster (`packages/services/valkey`) — prefer vault over env.
# node tools/hdc/cli.mjs secrets set HDC_VALKEY_PASSWORD
# HDC_VALKEY_PASSWORD=
