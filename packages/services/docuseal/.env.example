# Copy to packages/services/docuseal/.env in hdc-private (or hdc root; never commit).
# DocuSeal document signing — values optional unless manifest env_required is set.
# Prefer vault: node tools/hdc/cli.mjs secrets set <KEY>

# HDC_DOCUSEAL_DB_PASSWORD=
# HDC_DOCUSEAL_SECRET_KEY_BASE=
