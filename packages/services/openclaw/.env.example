# Copy to packages/services/openclaw/.env in hdc-private (or hdc root; never commit).
# OpenClaw personal AI agent — values optional unless manifest env_required is set.
# Prefer vault: node tools/hdc/cli.mjs secrets set <KEY>

# HDC_OPENCLAW_ANTHROPIC_API_KEY=
# HDC_OPENCLAW_GATEWAY_TOKEN=
