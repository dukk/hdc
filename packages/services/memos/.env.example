# Copy to packages/services/memos/.env in hdc-private (or hdc root; never commit).
# Memos notes — values optional unless manifest env_required is set.
# Prefer vault: node tools/hdc/cli.mjs secrets set <KEY>

# No documented HDC_* env vars for this package (vault-only or config.json).
