# Copy to packages/services/nginx/.env in hdc-private (or hdc root; never commit).
# Values are optional unless the package manifest declares env_required.

# Nginx web reverse proxy (`packages/services/nginx`) — prefer vault over env.
# node tools/hdc/cli.mjs secrets set HDC_NGINX_LE_EMAIL
# HDC_NGINX_LE_EMAIL=
