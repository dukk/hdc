---
description: HDC automation conventions — Node hdc CLI, inventory sidecars, secrets in .env only.
alwaysApply: true
---

# HDC automation

- Prefer the repo CLI: `node tools/hdc/cli.mjs`, or `hdc.cmd` (Windows) / `./hdc` (Unix, executable) from the repo root. Do not invent hostnames, IPs, or credentials.
- Structured facts live in `inventory/manual/{systems,networks,services,targets}/` as `*.json` (`kind`: `system`, `network`, `target`, or `services`). **System** records may list **`services`**: `{ "id": "<id>", "nodes"?: ["…"] }` pointing at **`kind: "services"`** sidecars under `inventory/manual/services/` (by `id` only, no inline service payload). Use **`kind` `target`** plus required **`automation_target`** (manifest id) for per-plugin sidecar data. Schemas under `tools/hdc/schema/inventory.*.schema.json`. Auth fields reference **env var names** only (for example `HDC_PROXMOX_SSH_USER`), never secret values.
- **System ids:** follow `.cursor/rules/hdc-inventory-naming.mdc` — class prefix (`vm-`, `ct-`, …) for non-physical systems; physical hosts unprefixed; instance letters (`-a`, `-b`) not numbers.
- Never commit or paste `.env` contents. After changing inventory JSON, run `node tools/hdc/cli.mjs docs lint` (or `docs sync` for the same checks). Optional companion `.md` files are not read or written by hdc.
- **Packages** (plugins) live under `packages/<package>/manifest.json` with `deploy/`, `maintain/`, and `query/` scripts (`run.mjs`). The directory will distinguish **service** packages (deploy specific apps) from **infrastructure** packages (shared capabilities such as VM/CT provisioning for other packages). Plugins may shell out to Ansible, Terraform, or SSH; orchestration stays in Node.
- **Logging:** For `hdc run <package> … query|deploy`, log user-visible progress on **stderr** only; keep **stdout** clean for JSON when the CLI parses it. See `.cursor/rules/hdc-automation-logging.mdc` (required checks for package `*.mjs` changes).
- **Masked secrets:** Vault passphrases, `hdc secrets set` values, and `getSecret()` prompts use **`readLineQuestion(q, { mask: true })`** (TTY raw mode, `*` echo) via `tools/hdc/lib/readline-masked.mjs`. Non-TTY falls back to visible input with a warning.
