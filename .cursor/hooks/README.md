# Cursor hooks (hdc)

No active hooks. Agent commits only when you explicitly ask.

The former `stop` auto-commit follow-up (`auto-commit-on-stop.mjs`) was removed so finishing a plan or agent turn does not request a Conventional Commit.
