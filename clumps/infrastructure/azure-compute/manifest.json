{
  "id": "azure-compute",
  "title": "Azure compute",
  "env_required": [
    "HDC_AZURE_COMPUTE_SUBSCRIPTION_ID",
    "HDC_AZURE_COMPUTE_TENANT_ID",
    "HDC_AZURE_COMPUTE_CLIENT_ID"
  ],
  "inventory_docs": [
    "inventory/manual/targets/azure-compute.json",
    "clumps/infrastructure/azure-compute/config.json"
  ],
  "verbs": {
    "deploy": { "script": "run.mjs" },
    "maintain": { "script": "run.mjs" },
    "query": { "script": "run.mjs" },
    "teardown": { "script": "run.mjs" }
  },
  "operation_report": {
    "next_steps": [
      "Run `hdc run infrastructure azure-compute query -- --live` after deploy.",
      "Review the Cost estimate section before the next deploy.",
      "Set inventory access IP for cloud VMs when assigned."
    ]
  }
}
