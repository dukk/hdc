# Copy to clumps/services/homepage/.env in hdc-private (or hdc root; never commit).
# Homepage (gethomepage.dev) — values optional unless manifest env_required is set.
# Prefer vault: node apps/hdc-cli/cli.mjs secrets set <KEY>

# HDC_HOMEPAGE_PROXMOX_API_TOKEN=
