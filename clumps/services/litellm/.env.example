# Copy to clumps/services/litellm/.env in hdc-private (or hdc root; never commit).
# LiteLLM (OpenAI-compatible AI gateway) — values optional unless manifest env_required is set.
# Prefer vault: node apps/hdc-cli/cli.mjs secrets set <KEY>

# HDC_LITELLM_DB_PASSWORD=
# HDC_LITELLM_MASTER_KEY=
# HDC_LITELLM_SALT_KEY=
